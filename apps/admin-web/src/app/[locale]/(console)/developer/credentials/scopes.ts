/** Curated `module:action` scopes offered when creating an API client. */
export const AVAILABLE_SCOPES = [
  'contacts:view',
  'contacts:edit',
  'segments:view',
  'campaigns:view',
  'campaigns:launch',
  'automation:view',
  'automation:edit',
  'jobs:view',
] as const;
