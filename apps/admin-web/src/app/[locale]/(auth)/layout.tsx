import type { ReactNode } from 'react';

// Intentionally empty shell — F0.7.T2 fills this in with the auth pages layout.
export default function AuthLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
