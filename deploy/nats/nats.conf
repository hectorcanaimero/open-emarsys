# NATS 2.11 con JetStream (NFR-2). Los streams se crean con `nats-init`
# (deploy/compose/infra/nats.yml) desde deploy/nats/streams/*.json.

port: 4222
http_port: 8222

jetstream {
  store_dir: /data
}
